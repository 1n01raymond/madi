{
  "name": "@madi/benchmark",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "benchmark": "pnpm run build && node dist/cli.js",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@madi/scene-ir": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "22.20.1"
  }
}
